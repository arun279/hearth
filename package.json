{
  "name": "hearth",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=10.18.0"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "check": "pnpm install --resolution-only && pnpm biome check . && pnpm check:md && pnpm check:typos && pnpm typecheck && pnpm check:types:boundaries && pnpm check:arch && pnpm check:knip && pnpm check:conventions && pnpm check:dup && pnpm check:env-example && pnpm db:check-auth && pnpm test && pnpm test:scripts && pnpm test:integration && pnpm test:bundle && pnpm check:coverage && pnpm check:licenses && pnpm audit --audit-level=high",
    "test:bundle": "turbo run test:bundle",
    "check:types:boundaries": "pnpm --filter @hearth/domain --filter @hearth/core --filter @hearth/ports typecheck",
    "check:conventions": "node scripts/check-conventions.mjs",
    "check:dup": "jscpd",
    "check:env-example": "node scripts/check-env-example.mjs",
    "check:licenses": "node scripts/check-licenses.mjs",
    "test:scripts": "node --test scripts/lib/*.test.mjs",
    "check:md": "dprint check",
    "check:typos": "node scripts/ensure-typos.mjs .",
    "format:md": "dprint fmt",
    "test": "turbo run test",
    "test:integration": "turbo run test:integration",
    "test:coverage": "turbo run test:coverage",
    "check:coverage": "turbo run test:coverage",
    "e2e": "turbo run e2e",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check:arch": "NODE_OPTIONS=--max-old-space-size=4096 depcruise --config .dependency-cruiser.cjs packages apps",
    "check:knip": "knip",
    "dev": "turbo run dev",
    "db:generate": "pnpm --filter @hearth/db db:generate",
    "db:migrate:dev": "pnpm --filter @hearth/db db:migrate:dev",
    "db:check-auth": "pnpm --filter @hearth/db db:check-auth",
    "approve-email": "node scripts/approve-email.mjs",
    "bootstrap-operator": "node scripts/bootstrap-operator.mjs",
    "local-session": "node scripts/local-session.mjs",
    "seed-local": "node scripts/seed-local.mjs",
    "restore:fts": "node scripts/restore-fts.mjs",
    "prepare": "lefthook install"
  },
  "pnpm": {
    "overrides": {
      "drizzle-zod>zod": "^4.1.11",
      "@esbuild-kit/core-utils>esbuild": "^0.25.0",
      "@esbuild-kit/esm-loader>esbuild": "^0.25.0",
      "ajv>fast-uri": "^3.1.2",
      "better-auth>kysely": "^0.28.17"
    }
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "dependency-cruiser": "^16.10.1",
    "dprint": "^0.54.0",
    "jscpd": "^4.0.9",
    "knip": "^6.0.0",
    "lefthook": "^2.1.8",
    "turbo": "^2.9.5",
    "typescript": "catalog:"
  }
}
